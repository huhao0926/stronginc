运行环境 :
cmake,mpi,boost >=1.8
gflag,glog输出日志文件
cpp文件夹是所有的核心代码bfs,connectivity,d_hop_nodes,dualsimulation and strongsimulation(two graph matching algorithm),dual simulation incremental,strong simulation incremental.

parallel bfs,connectivity,dualsimulation,strong simultaion.
app是测试代码